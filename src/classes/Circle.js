import Collisions from '../Collisions.js';

export default class Circle {
	constructor(x = 0, y = 0, radius = 0, scale = 1) {
		this.x      = x;
		this.y      = y;
		this.radius = radius;
		this.scale  = scale;
	}
}
